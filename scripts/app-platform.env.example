# Copy to scripts/app-platform.env and fill in secrets as needed.
DATABASE_URL=postgresql://postgres@do-db:5432/einstore?schema=public
NODE_ENV=production
PORT=8100

SPACES_ENDPOINT=https://lon1.digitaloceanspaces.com
SPACES_REGION=lon1
SPACES_BUCKET=einstore
SPACES_KEY=changeme
SPACES_SECRET=changeme

GOOGLE_CLIENT_ID=changeme
GOOGLE_CLIENT_SECRET=changeme
GOOGLE_REDIRECT_URI=https://api.einstore.pro/auth/oauth/google/callback

AUTH_JWT_SECRET=changeme
AUTH_JWT_ISSUER=einstore
AUTH_JWT_AUDIENCE=einstore-api
AUTH_REFRESH_TTL_DAYS=30
AUTH_ACCESS_TTL_MINUTES=15

API_KEY_SECRET=changeme
CORS_ORIGINS=https://admin.einstore.pro,https://einstore.pro,http://localhost:8101

ENABLE_SCHEMA_CREATE=false
RUN_PRISMA_DEPLOY=false

VITE_API_BASE_URL=https://api.einstore.pro
